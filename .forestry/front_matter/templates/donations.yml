---
label: Donations
hide_body: true
fields:
- type: field_group_list
  name: single
  label: single
  fields:
  - type: text
    name: amount
    label: amount
  - type: text
    name: url
    label: url
  config:
    labelField: amount
- type: field_group_list
  name: monthly
  label: monthly
  fields:
  - type: text
    name: amount
    label: amount
  - type: text
    name: url
    label: url
  config:
    labelField: amount
pages:
- _data/donations.yml
