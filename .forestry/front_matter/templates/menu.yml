---
label: Menu
hide_body: false
fields:
- name: current
  type: select
  default: []
  config:
    required: false
    options: []
    source:
      type: documents
      section: configuration
      file: _data/menus.yml
      path: 
  label: current
- type: number
  name: weight
  label: weight
- name: menus
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: documents
      section: configuration
      file: _data/menus.yml
  label: menus
